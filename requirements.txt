# DropCat Studio — Python dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
Pillow>=10.0.0
ollama>=0.4.0

# Cloud AI providers (required if using Anthropic/OpenAI instead of Ollama)
anthropic>=0.25.0
openai>=1.20.0

# VOID worker dependencies (services/void_worker.py)
torch>=2.0.0
diffusers>=0.28.0
numpy>=1.24.0

# Optional — gracefully skipped if missing
opencv-python>=4.9.0
pystray>=0.19.0
tqdm>=4.60.0
